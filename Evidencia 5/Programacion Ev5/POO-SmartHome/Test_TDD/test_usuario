import unittest
from core.usuario import Usuario

class TestUsuario(unittest.TestCase):
    def test_verificar_password(self):
        user = Usuario("Juan", "1234", "admin")
        self.assertTrue(user.verificar_password("1234"))
        self.assertFalse(user.verificar_password("abcd"))

if __name__ == "__main__":
    unittest.main()